spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true       # SQL 쿼리 출력
    properties:
      hibernate:
        format_sql: true  # 쿼리 예쁘게 출력
        use_sql_comments: true  # (선택) 쿼리에 주석 추가